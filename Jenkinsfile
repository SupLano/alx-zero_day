pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Consider adding more meaningful build steps here
            }
        }
    }
}
